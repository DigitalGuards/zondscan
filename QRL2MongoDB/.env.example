# Each process uses one network and one MongoDB database.
EXPLORER_NETWORK=v2
MONGO_DB_NAME=qrldata-z
# Optional paired pins for v2. Both are mandatory for v3.
# EXPECTED_CHAIN_ID=<chain-id>
# EXPECTED_GENESIS_HASH=<0x-prefixed-genesis-hash>
# v3 additionally requires a reviewed 64-byte build and a separate database.

# An optional URI database suffix must match MONGO_DB_NAME.
MONGOURI=mongodb://localhost:27017

# Zond RPC endpoint for block synchronization
NODE_URL=http://localhost:8545

# Optional: Separate RPC endpoint for mempool/pending transaction detection
# Useful when using a public RPC for blocks but local node for mempool access
# Falls back to NODE_URL if not set
MEMPOOL_NODE_URL=http://localhost:8545

# Optional: index native value transfers made inside contract execution.
# Keep the debug namespace private. A loopback-only WebSocket endpoint lets
# the public HTTP RPC retain its restricted module list.
ENABLE_DEBUG_TRACE=false
# TRACE_NODE_URL=ws://127.0.0.1:8546

# Pinned syncers also verify the independent beacon source.
# EXPECTED_BEACON_GENESIS_VALIDATORS_ROOT=<0x-prefixed-beacon-genesis-root>
# EXPECTED_BEACON_GENESIS_TIME=<canonical-decimal-genesis-time>

# Beacon chain HTTP API endpoint for validator data
BEACONCHAIN_API=http://localhost:3500

# Optional: RPC delay configuration (milliseconds)
# RPC_DELAY_MS=50
# RPC_DELAY_JITTER_MS=26
